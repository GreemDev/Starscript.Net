# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         gruke --generate-configuration GitHubActions_benchmarks --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: benchmarks

on:
  push:
    branches:
      - main
    paths:
      - 'Starscript.Net.sln'
      - 'build/**'
      - 'src/Lib/**'
      - 'src/Benchmarks/**'
      - '!.github/**'
      - '!docs/**'
      - '!assets/**'
      - '!.vscode/**'
      - '!*.yml'
      - '!*.json'
      - '!*.md'
      - '!.gitignore'
      - '!.gitattributes'
  pull_request:
    branches:
      - main
    paths:
      - 'Starscript.Net.sln'
      - 'build/**'
      - 'src/Lib/**'
      - 'src/Benchmarks/**'
      - '!.github/**'
      - '!docs/**'
      - '!assets/**'
      - '!.vscode/**'
      - '!*.yml'
      - '!*.json'
      - '!*.md'
      - '!.gitignore'
      - '!.gitattributes'

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v5
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Benchmarks'
        run: ./build.cmd Benchmarks
